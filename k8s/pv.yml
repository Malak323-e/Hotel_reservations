apiVersion: v1
kind: PersistentVolume
metadata:
  name: hotel-db-pv
spec:
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  persistentVolumeReclaimPolicy: Retain
  hostPath:
    path: /data/hotel-db
